#include "decode.h"
#include <iostream>
void decode(Lista *lista)
{
	Wezel* korzen = lista->pierwszy;
	string decoded;
	vector<string> v = lista->nazwy;
	for (int i = 0; i < v.size(); i++)
	{
		v[i] = decoded;

}